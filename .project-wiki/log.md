# Project Wiki Log

本页保留历史确认与修订，不作为当前需求正文。当前规则按[主题索引](index.md)查阅；
旧记录中的待定或已替代口径以主题页记录的后续确认为准。

| Date | Change |
| --- | --- |
| 2026-09-21 | 上游模板建立通用流程、工程原则和知识规范；历史来源见[来源记录](../docs/provenance.md) |
| 2026-09-22 | 保存 SQL Baseline 原文和来源版本，标明其为待讨论的原始项目知识 |
| 2026-09-22 | 按用户授权采用 agent-harness 建立本地仓库；整合入口和知识索引，业务方案继续待确认 |
| 2026-09-22 | 按用户授权公开发布至 shenxg13/sql-apm；首次推送、六个状态标签、连接器读取及 CI 已验证，见[发布记录](../docs/reports/initial-publication.md) |
| 2026-09-22 | 用户确认 Python 精确版本为 3.9.5，后续在项目 `.venv/` 中建立环境；约束及来源见[知识索引](index.md)，环境创建尚未执行 |
| 2026-09-22 | 用户确认 Baseline 存储采用 PostgreSQL 17，后续按需使用 PostgreSQL 9.4.26 做实时采集兼容测试；同步知识索引和开发说明，数据库尚未部署 |
| 2026-09-22 | 用户确认首期不引入 Prometheus；采用自研 Python、PostgreSQL 17 和 Grafana，复用 HashData 已有监控，平台状态先保留任务记录和日志；通知方案待后续确认 |
| 2026-09-22 | 记录用户提供的容量口径：现网单一 Master 的 SQL CSV 未压缩约 4 GB/天，现有两份样本来自测试环境；留存期限及去重归档方案待确认 |
| 2026-09-22 | 用户明确暂缓 SQL 原文存储、去重压缩及留存方案讨论，后续视生产日志能否获取再评估；测试样本统计仅作参考 |
| 2026-09-22 | 用户确认首期手动拷贝 SQL 日志至基线服务器，不引入流式读取组件；后续通过简单脚本定时 SCP，每天传输一次；具体传输与导入细节待确认，自动传输尚未实现 |
| 2026-09-22 | 用户确认 SQL 采用默认纳入、黑名单排除的策略；初始排除 SET、BEGIN、COMMIT、VACUUM、ANALYZE、CREATE INDEX 和 ALTER TABLE，其他未明确排除的语句默认纳入；样本质量规则和存储策略另行确认 |
| 2026-09-22 | 用户要求不同执行用户分别建立基线；基本分组确认为计算集群＋数据库＋执行用户＋SQL 结构指纹，同一用户下的 schema / search_path 差异及标识字段映射待进一步确认 |
| 2026-09-22 | 用户确认首期保留现有四维分组，指纹保留显式 schema 和表名；不推断隐含 schema，暂不完整还原 search_path 等会话状态，并接受不同隐含 schema 的执行可能混入同一基线的已知限制 |
| 2026-09-22 | 用户说明现网日志记录所有已完成 SQL 的耗时，而非仅记录超过时长门槛的 SQL；作为用户提供的事实保存，具体配置及生产覆盖情况尚未独立核验，训练样本筛选规则待确认 |
| 2026-09-22 | 用户确认基本训练资格：成功完成、耗时完整且执行关联可靠的记录进入候选集；明确失败、取消和超时不参与正常耗时训练，状态不明或信息不可靠的记录暂不参与；成功长耗时执行的异常筛选及具体日志映射规则待后续确认 |
| 2026-09-22 | 用户确认首期需要按计算集群及手动指定的故障或维护时间段排除受影响的训练样本；具体时间匹配规则、操作入口和对已有基线的处理方式待确认，功能尚未实现 |
| 2026-09-22 | 用户确认故障或维护时段按执行区间重叠匹配：指定计算集群内，SQL 执行期间只要与排除时段有重叠即不参与训练，包括在时段前启动或跨越整个时段的执行；时区、端点边界及对已有基线的处理方式待确认 |
| 2026-09-22 | 用户明确首期日志事后录入、离线构建，暂不需要基线生成后因补录排除时段而重算的流程；在本轮构建前准备日志及已知排除时段，保留已确认的人工排除和区间重叠规则 |
| 2026-09-22 | 用户希望首期统计维度更丰富；结合前文按丰富每组基线的统计信息理解，记录样本覆盖、耗时分布、波动、稳健统计、长尾、样本质量和时间变化的候选清单；具体指标与计算口径待确认，不自动新增分组维度或按时段独立建模 |
| 2026-09-22 | 用户确认每日批量日志完整导入后构建新基线，检查成功后切换生效，失败时继续使用原版本并显示最后更新时间；保留当前及历史版本的统计结果和构建信息，不按版本复制执行明细或 SQL 原文；训练窗口长度和历史版本保留期限仍待确认，补录排除时段后的追溯重算继续暂缓 |
| 2026-09-22 | 用户确认训练窗口默认最近 30 个自然日、允许调整，以每次构建指定的历史截止日为终点；数据不足时先生成可计算的统计并注明实际覆盖范围；样本量及基线可用性门槛、各类数据与历史版本保留期限继续待定 |
| 2026-09-22 | 用户暂定采用可调整的样本门槛，后续根据实际数据修订：每组基础条件为有效执行至少 30 次且活跃至少 7 天，P95 和 P99 用于异常判断分别要求至少 200 次及 1,000 次且均覆盖 7 个活跃日；未达门槛仍展示可计算统计并注明不足，达到门槛仍需质量检查，不视为稳定性保证 |
| 2026-09-22 | 用户确认首期保留通过基本筛选、未命中已确认排除规则的成功长耗时执行参与统计，不仅因耗时长就自动剔除；疑似离群或高波动标记不自动排除样本，具体标记及可用性规则待定，自动离群剔除算法留待后续实际数据分析 |
| 2026-09-22 | 用户确认首期导入已结束写入且复制完成的完整日志文件；同一来源、相同内容且已成功导入的文件再次提交时跳过，改名不重复计数；导入中断允许安全重试，批次未完整导入不发布新基线；保留 SQL 真实重复执行，文件追加及跨文件内容重叠的处理继续待定 |
| 2026-09-22 | 用户确认导入异常按影响范围处理：可可靠定位的问题记录记明原因，排除受影响执行后继续处理其他可靠记录；文件损坏导致记录边界不可靠时导入失败，本批次不发布新基线；汇总问题数量及定位信息，日志内正常记录的 SQL 执行报错不算导入程序失败，发布所需质量门槛仍待确认 |
| 2026-09-22 | 用户确认统一以北京时间（UTC+8）划分自然日、展示时间及填写故障或维护时段；日志按来源时区解析后转换，跨天 SQL 按执行结束日归入训练窗口、活跃天数和按天统计，保留完整耗时；故障排除仍按完整执行区间重叠，小时归属、端点边界及来源时间字段映射待落实 |
| 2026-09-22 | 用户提出开始时间更适合与后续 activity 比较；记录统一调整训练窗口、活跃天数、按天及按小时归属的修订意向，整体范围尚待确认，未将建议覆盖为已确认规则 |
| 2026-09-22 | 用户补充生产 HashData 的单次 SQL 可能持续数小时；只读复查现有两份测试日志，记录会话及命令编号关联的反例、日志时间差与 duration 不等的候选记录对及输入校验值；测试最大耗时记录约 38 秒，不能据此宣称数小时执行或生产关联已验证，具体解析及跨文件上下文方案仍待落实 |
| 2026-09-22 | 应用户要求摘取测试日志实例：记录逻辑记录 88、89 对应的 execute 和 duration 消息及物理行定位；明确 Parse/Bind/Execute 分阶段耗时来自 PG 文档，两份样本的 duration 消息未显式标注阶段，不能以通用机制替代实际阶段映射验证 |
| 2026-09-22 | 用户接受首期忽略 Parse、Bind 耗时影响，以选定完成记录的结束时间和 duration 推算开始时间；记录推算性质，不要求为取得开始时间配对独立开始日志或精确重建协议阶段，故障/维护排除使用推算区间，完成记录识别及既定训练资格仍适用；补记用户关于按天和按小时统一采用结束时间的讨论，按小时归属尚待明确确认 |
| 2026-09-22 | 核对既有测试日志检查结果：两份文件的 196,618 条 duration 记录均在同条 CSV 的第 25 列携带 SQL 文本，消息字段仅有耗时不表示 SQL 缺失；记录存在性证据，未将其扩大为文本无截断、参数完整或生产日志相同的保证 |
| 2026-09-23 | 应用户要求全量核查两份测试日志的 SQL 文本与绑定参数：未发现明显截断迹象；294 条耗时记录含占位符但无参数明细，98 条带占位符的 execute 记录在 DETAIL 中覆盖全部引用参数编号；保存只读核查报告、输入校验值及有界复查位置，不将日志内部一致性扩大为客户端原文保真或生产保证，不新增参数还原需求 |
| 2026-09-23 | 用户确认首期保留 SQL 参数占位符，暂不从 execute DETAIL 恢复实际绑定参数；以选定完成记录的 SQL 文本生成结构指纹、以 duration 建立耗时基线，不将参数明细的精确关联作为前置条件，缺少实参不单独排除样本；明确占位符文本的展示边界，其余训练资格及日志留存安排不变 |
| 2026-09-23 | 用户明确选择统一按 SQL 结束时间归属：训练窗口、活跃天数及按天统计采用北京时间的结束日期，小时画像采用结束小时；跨天、跨小时执行保留完整耗时，不拆分样本，推算开始时间用于执行区间展示及故障／维护排除；后续 activity 分时匹配规则仍待确认 |
| 2026-09-23 | 用户在讨论 activity 比对及启动时间偏移后确认整体修订时间口径：训练窗口、活跃天数、按天统计与小时画像统一按推算开始时间的北京时间日期或小时归属，替代此前按结束时间归属的决定；同步活跃天数定义，跨日或跨小时仍保留完整耗时，完成并导入且满足训练资格后才参与基线，晚到完成日志在开始日期仍处于训练窗口时纳入后续构建；保留推算标记及区间重叠排除，activity 分时匹配、邻近时段参考与整体基线回退规则仍待确认 |
| 2026-09-23 | 用户提供单一 Master 的 7 个生产日志及轮转文件，确认 CST 为北京时间，并在核对末条记录后确认 9 月 20 日后续日志未包含；完成全量 CSV 取证及有界复查，记录 2,499,534 条逻辑记录、1,103,323 条耗时记录、可定位的编码与文本完整性问题、分钟级显式 Parse/Bind、多语句文本及压缩／原文去重测量，见[生产分析报告](../docs/reports/production-log-analysis-2026-09-23.md)；未把耗时记录数等同执行数，未改变时间归属、训练资格、忽略协议阶段的已确认假设或留存方案，相关适用范围及完成记录识别继续讨论 |
| 2026-09-23 | 用户提供日志配置 log_duration=on、log_min_duration_statement=1min、log_statement=all、log_min_error_statement=error，并明确现网版本已在原始需求中记录；核对为 PostgreSQL 9.4.26、Greenplum Database 6.20.3、HashData Warehouse 3.13.13，提升这些环境事实到当前项目知识，保留原文快照；参数值未独立读取，多语句计时范围及完成记录映射尚未实测，不将本次 psql 批次测试建议记为已执行或将整体指纹方案记为已确认 |
| 2026-09-23 | 用户提供 psql 多语句测试的两张终端照片：单个 -c 请求执行 pg_sleep(2) 与 pg_sleep(5)，正常返回，并可见携带两条 SQL 的 duration=7031.575 ms，支持本次路径的该条记录按整批计时；保存有界案例及证据限制，截图经 grep pg_sleep 过滤且原始测试 CSV 未取得，不能证明没有其他耗时记录或推广到全部协议；整体批次指纹和混合批次黑名单仍是待确认建议，不将实验观察自动提升为产品决策 |
| 2026-09-23 | 用户明确选择“采用上述规则，混合批次可建整体基线”：单条 SQL 与已可靠识别完整成功执行及整批耗时的多语句请求分别统计，批次按语句顺序生成整体结构指纹，一次批次执行计一个样本，不向子语句分配总耗时；纯黑名单批次排除，包含未被黑名单排除的业务语句的混合批次允许整体纳入，其控制和维护耗时保留，其他训练资格及故障时段排除继续适用；计时归属不清暂不训练，具体完成记录识别和常量／注释／配置值规范化仍待落实 |
| 2026-09-23 | 用户确认“业务常量归一化，按结构合并”：同一 SQL 或批次结构相同，仅 WHERE 条件中日期、订单号等显式业务常量不同，替换为占位标记并归入同一指纹，首期接受这些参数引起的耗时波动混入同组；不合并真实执行、不改原始输入、保留对象名及批次顺序，SET 配置值、LIMIT 数量等特殊情况和具体规范化算法继续待定 |
| 2026-09-23 | 用户确认“保留这些特殊值，分别统计”：混合批次 SET 配置值及 SQL 显式 LIMIT／OFFSET 数量保留，不同值分别生成指纹并统计，接受更多分组；范围限于日志 SQL 中显式出现的值，不要求还原完整会话配置，不改变独立 SET 黑名单或原有参数占位符边界，其他规范化细节仍待落实 |
| 2026-09-23 | 用户确认“忽略普通格式和说明性注释”：不影响语义的空格、换行、SQL 关键字大小写及普通说明性注释不改变指纹；双引号内对象名保留大小写，可能影响执行的 Hint 类特殊注释不删除，具体识别规则待落实；不改写原始日志、不改变批次顺序或已确认的业务常量和特殊值边界 |
| 2026-09-23 | 用户确认首期时间统计采用整体、按天及跨天小时画像三层，不额外保存逐日逐小时汇总；小时画像按北京时间的推算开始小时，将训练窗口内各天同一小时的有效已完成执行合并统计，单条／批次单位及跨界完整耗时规则保持不变；具体指标、空时段表示和小时画像判断门槛待定，不自动确认 activity 匹配或原文与明细留存方案 |
| 2026-09-23 | 用户澄清两种小时统计含义后，引用“跨天小时画像”并要求“首期先只保留这个”；按该轮小时方案对比的范围记录为仅保留跨天小时画像、不增加逐日逐小时汇总，不扩展为取消既有整体／按天统计；三层是否均保存完整指标仍未确认 |
| 2026-09-23 | 用户进一步归纳并核对“全部，逐天，跨天逐小时，三个维度保留全部主要指标”；按本次归纳将三层统一保留此前讨论的主要指标记录为当前口径，替代整体丰富、日／小时精简的候选方案，包括样本覆盖、耗时分位分布、标准差／CV、MAD／IQR、对数中位数／MAD、长尾比值及可归属的样本质量信息；各层按自身样本计算，具体公式、不可算值表示及日／小时异常应用条件仍待落实，不新增逐日逐小时汇总或原文留存决定 |
| 2026-09-23 | 用户确认跨天小时画像的独立样本门槛：每个基线分组、训练窗口及开始小时，基础至少 30 次、P95 至少 200 次、P99 至少 1,000 次，均需该小时覆盖至少 7 个北京时间推算开始日期；不借用整体或其他小时的数量，分别标记基础与 P95／P99 条件，未达门槛仍保存可计算指标并注明不足，不用于自动异常判断；数值可配置、后续校准且不保证稳定性，activity 小时匹配、邻近时段参考及整体回退规则继续待定 |
| 2026-09-23 | 用户确认后续 activity 基线参照采用“开始小时优先，不足时回退整体”：在同一计算集群、数据库、执行用户及 SQL 指纹组内，优先使用 SQL 开始小时的可用画像，小时不足但整体可用时回退整体并标注来源；两者都不足时展示已运行时长及基线不足，暂不自动判断异常，不把依据不足当作已判定正常；可用性仍须满足所用指标的样本门槛与后续质量条件，邻近时段额外参考、具体字段／指纹匹配及异常判定方法待定，未实现 activity 采集 |
| 2026-09-23 | 用户确认排除计数采用“总数按执行去重、各原因分别计数”：同一统计范围内，一次执行只计入排除总数一次，在同一原因下也只计一次，可同时计入多个原因，因此原因数量之和可能大于总数；沿用单条／整批执行单位，不按 SQL 文本或指纹合并真实重复执行，无法可靠识别或归属的问题仍在导入批次层面统计；同步三层主要指标中的总数与原因计数，具体功能尚未实现 |
| 2026-09-23 | 用户确认部分 SQL 分组或指标样本不足不阻止整个新基线版本生效：不足项目保留可计算统计并标记，未达门槛的项目不用于自动异常判断，满足使用条件的项目正常提供基线参考；每日导入批次完整、构建及发布检查通过仍是生效前提，否则继续使用原版本；全部分组均不足或训练窗口内无有效样本时的生效策略仍待确认，未实现构建或发布功能 |
| 2026-09-23 | 用户确认全部样本不足与零样本的版本生效边界：训练窗口内有有效样本但所有分组未达门槛时，其他检查通过后可生效，保留统计及不足标记，暂不自动判断异常；整个训练窗口经筛选后有效样本为零时，记录空结果及原因，不切换版本，保留原版本并显示本次未更新，没有原版本时显示暂无基线；零样本不以当天新导入量判断，不绕过批次完整性及构建发布检查，功能尚未实现 |
| 2026-09-23 | 用户确认首期批次完整性采用“人工确认文件齐全，程序核对处理完成”：手动导入时指定来源、覆盖日期及文件并确认已拷齐，程序记录清单，全部完成导入或按既定成功导入去重规则跳过后才进入基线构建；本地文件名、存在性或末条日志时间不能证明源端没有漏拷，原有导入异常处理和发布检查继续适用；文件清单格式、标识与具体入口仍待落实，功能尚未实现 |
| 2026-09-24 | 用户确认首期统一入口先覆盖 SQL 检索、基线和执行历史展示，并表达优先采用 Grafana 的偏好；保留既有 Grafana 展示选型，具体插件与接口尚待落实，Business Forms、自定义 App 插件和 Appsmith 仅为候选；本次不增加导入、构建、黑名单和排除时段的网页管理要求，不改变尚未确认的原文与历史留存策略；尚未实现界面或检索服务 |
| 2026-09-24 | 用户明确确认 Grafana 插件作为首期统一入口的主要实现候选，范围仍为 SQL 检索、基线和执行历史展示；Business Forms 与自定义 App 插件为待评估的具体方案，Appsmith 保留为未选定替代方案；尚未选定具体插件、接口或版本，未完成交互与兼容性验证，未实施界面或部署组件 |
| 2026-09-24 | 用户要求 Business Forms 与自定义 Grafana App 插件均保留为候选，以维护简单为选型优先条件，并倾向 Business Forms；记录为在满足首期展示范围的前提下优先评估 Business Forms，同时保留自定义 App 方案；尚未选定具体插件，未验证本项目中的维护成本、兼容性或交互效果 |
| 2026-09-24 | 用户确认首期允许直接粘贴完整 SQL 或完整批次，复用日志入库归一化规则按结构指纹检索；计算集群、数据库和执行用户为可选筛选，命中多个分组时分别列出供选择，选定后展示该组基线与执行历史，不自动合并不同分组；具体算法、接口和界面尚未实现，原文及明细留存和其他检索方式仍待讨论 |
| 2026-09-24 | 用户确认 SQL 详情页默认展示当前生效基线，支持切换已保留的历史版本，并显示训练窗口、构建时间及样本不足状态；执行历史查询时间范围独立选择，调整它仅改变历史记录展示，不重建或重算所选基线；查看历史版本不改变系统当前生效版本，留存期限仍待确认，页面与接口尚未实现 |
| 2026-09-24 | 用户确认执行历史保留并展示全部能可靠识别且归属到 SQL 分组的执行，包括成功、失败、取消、超时及因故障／维护等规则未参与训练的执行，标明状态和已知排除原因；耗时缺失或不可靠显示未知，不补零；无法可靠识别或归属的日志问题保留在导入批次层面，训练资格与单条／批次粒度继续适用；具体留存期限及 SQL 原文存储方式仍待讨论，功能尚未实现 |
| 2026-09-24 | 用户提供 119、120 两集群日志并确认版本及三个日志参数相同、分别来自单一 Master、文件已关闭且拷贝完整、北京时间；完成 46 个文件约 11.32 GB 的全量只读扫描和有界复查，保存[两集群报告](../docs/reports/cluster-log-analysis-2026-09-24.md)及输入校验值。用户确认 119 在 7 月 1 日上午至 4 日的空档与当时停机／切换／未写日志有关；记录来源分类线索、显式分钟级 Parse／Bind、主完成候选文本质量、原文分组覆盖及存储测量。120 仅涉及最多五个推算开始日期，不能满足七活跃日门槛；不把精确原文分组当成有效基线，不将阶段提议自动加入正式分组，不改变忽略阶段假设或留存决策，未实施导入器或基线构建 |
| 2026-09-24 | 用户要求在无法提供 HashData 私有源码的条件下核查 1946／2843／2219／2603；从官方 GP 归档固定提交取得 6.20.3 开发谱系源码，结合 46 文件定向扫描形成[高置信语义映射](../docs/reports/hashdata-duration-source-mapping-2026-09-24.md)：Simple Query／Execute／Parse／Bind。补充发现 119 有 13,737 条、120 有 277 条 execute fetch from，对应耗时也来自 2843；有界复查确认首次与续取都可计时，因此不能把每条 2843 直接当成完整 SQL，或只过滤续取后保留首段。记录上游活动开始时间按协议消息更新的边界，未宣称 HashData activity 已对齐，未自动累加阶段、增加阶段基线或实现解析器 |
| 2026-09-24 | 用户询问四类之外的源码位置；对同一批 46 文件清点并有界复读，确认 duration 共 8 个位置，另有 1455／2224／2608／autostats.c:334，其中详细 Parse／Bind 与原简略分支属于相同阶段；postgres.c 全部消息合计 17 个位置，记录其计数与级别，说明文件名＋行号不同于固定四位编码。证据补入[专项报告](../docs/reports/hashdata-duration-source-mapping-2026-09-24.md)，未改变基线规则或实现解析器 |
| 2026-09-24 | 用户询问 2843 首次与续取能否精确关联；复核上游 Execute／Close／Bind 路径并有界复读两个案例，确认 2764 可提供具名 portal 线索而简略 2843 不包含该标识，120 案例命令号会变化。区分单次配对、portal 调用链和完整成功执行三层证据；现有续取配对计数未验证全量调用链，最后 duration 不证明取数完成。证据补入[专项报告](../docs/reports/hashdata-duration-source-mapping-2026-09-24.md)，未实现关联器或更改训练资格 |
| 2026-09-24 | 用户追问 2843 是否无完整 SQL 及与 2764 单次配对精度；澄清 message 仅有 duration 与 CSV 独立 SQL 字段有文本可以同时成立，两集群全部 2843 均有非空 SQL，但 120 有 121 条词法闭合问题候选。复读两例独立字段，汇总既有候选配对覆盖为 119 的 671,465／671,465、120 的 457,064／457,073；明确覆盖率不等于准确率，单次配对不等于完整 portal 链。修正[专项报告](../docs/reports/hashdata-duration-source-mapping-2026-09-24.md)及知识索引的易误解表述，未实现配对器或更改已确认规则 |
| 2026-09-24 | 用户确认 Execute 首期按单次调用关联 2764 与 2843：2843 提供独立 SQL、duration 和结束时间，2764 提供首次／续取分类；可靠配对后在原四维 SQL 分组内分别统计，无法可靠配对标为类型未知，不强行归类。一次配对计一个调用样本，不要求完整 portal 链重建，不将续取次数或调用和当作完整 SQL 执行。同步[分组与调用分类规则](contracts/timing-and-grouping.md#已确认的-execute-单次配对与调用分类统计)和 Agent 入口；Parse／Bind 单独统计仍待确认，调用级指标门槛及版本衔接未落实，原三层业务基线与主要指标保持既有口径，尚未实现 |
| 2026-09-24 | 用户明确将 Parse 和 Bind 加入首期，继续使用计算集群、数据库、执行用户、SQL 结构指纹、计时类别五项 GROUP BY。计时类别现为请求整体、Execute 首次、Execute 续取、Parse、Bind；Parse 合并 2219／2224，Bind 合并 2603／2608 的同类样本。样本不足保留可计算指标和覆盖／不足提示，仅供观察，不作自动异常判断，各类别独立解释样本量；阶段调用数不作为完整执行数，不自动累加阶段耗时。同步[五类统计口径](contracts/timing-and-grouping.md#已确认的五类计时统计与-parsebind-纳入)、Agent 入口与开发说明，保留三层时间框架，具体阶段门槛、公式与版本衔接待落实，功能未实现 |
| 2026-09-24 | 用户询问已确认五类计时对 duration 日志的覆盖；从既有 46 文件全量计数计算，119 为 2,826,862／2,854,821（99.02%），120 为 2,841,641／2,929,866（96.99%），合计 5,668,503／5,784,687（97.99%）。未覆盖 1455 和 autostats.c:334；扣除 120 的 9 条未候选配对 Execute 后为 5,668,494 条，四舍五入仍 97.99%。补入[覆盖率说明](../docs/reports/hashdata-duration-source-mapping-2026-09-24.md#已确认五类计时的日志覆盖率)，区分源码位置覆盖、候选分类覆盖与可训练样本覆盖，本轮未重扫日志或改变统计规则 |
| 2026-09-24 | 用户表示“这个覆盖率我觉得可以了”，接受现有五类计时在 46 文件中约 97.99% 的覆盖，首期按五类推进，1455 和 autostats.c:334 暂不扩展；未纳入及无法分类记录保留诊断数量和已知原因，未可靠配对 Execute 不强行归类。同步[首期覆盖范围](contracts/timing-and-grouping.md#已确认的首期计时覆盖范围)、Agent 入口及专项报告；该比例不固定为未来验收阈值，不代表已验证配对准确率或训练样本覆盖率，尚未实现分类器与统计 |
| 2026-09-24 | 用户确认把 INSERT … VALUES 和 UPDATE … SET 中直接作为列值或赋值的业务字面常量也纳入归一化，同结构且仅这些值不同的 SQL 使用同一结构指纹；保留对象名、列名、表达式结构、显式类型转换和批次顺序，SET 配置及 LIMIT／OFFSET 具体值继续保留。同步[业务常量规则](contracts/sql-fingerprints.md#已确认的业务常量归一化)、Agent 入口与开发说明；其他语法位置、占位标记及原生参数的归并细节仍待落实，未实现指纹或修改原始输入 |
| 2026-09-24 | 用户确认在已允许归一化的 WHERE 业务值及 INSERT／UPDATE 直接列值位置，将原生参数占位符与业务字面常量归为同一结构指纹，SQL 检索复用该规则；源 SQL 保留占位符且不还原绑定值，对象、表达式结构和显式类型转换须一致，SET 配置及 LIMIT／OFFSET 保留规则、五项分组和单条／整批粒度继续适用。同步[参数与常量归并](contracts/sql-fingerprints.md#已确认的原生参数与业务常量归并)、Agent 入口及开发说明；具体指纹实现和其他未确认细节仍待落实，未修改原始输入 |
| 2026-09-24 | 用户要求创建 Issue，后续通过实施维护首版函数归一化字典；已在 shenxg13/sql-apm 创建[Issue #1](https://github.com/shenxg13/sql-apm/issues/1)，包含目标、范围与非目标、拟定验收项、验证计划及开放问题，并回读核对正文和唯一 status:triage 标签。首版名单与完整契约尚待在 Issue 中明确，未开始实施或确认全部示例规则；知识索引、Agent 入口和开发说明仅记录入口及推进方式，执行状态仍由 GitHub 管理 |
| 2026-09-24 | 用户确认五类计时、三层统计的耗时数值统一使用毫秒并保留小数，比值指标无单位；可靠日志零值保留且仍须满足样本资格，缺失或不可靠耗时用 NULL 表示未知。无有效样本时有效样本数为 0、耗时指标为 NULL 并注明无样本，分母为零等无法计算指标也用 NULL 并注明原因；样本不足仍保留可计算数值及不足标记。同步[数值口径](contracts/baseline-statistics.md#已确认的耗时单位与统计空值口径)、Agent 入口及开发说明；对数零值算法和具体公式仍待确认，不改变整窗无有效样本的发布规则，尚未实施存储与统计 |
| 2026-09-24 | 用户要求“后续完成确认意向后就开始确认下一项”；后续需求沟通在确认并记录当前事项后，同一轮直接提出下一项实质待确认内容，不再只回复已记录后等待再次催促。已同步 Agent 入口；此沟通节奏不将后续提议自动视为确认，也不启动产品实施 |
| 2026-09-24 | 用户确认五类计时、三层统计统一采用包含端点的线性插值分位数（位置 1+(n−1)p）、除以 n 的总体标准差及对应 CV、未缩放 MAD、IQR，以及固定毫秒数值的 ln(1+x) 变换后中位数和未缩放 MAD；单样本保留分位数及零离散度并标不足，无样本和零分母沿用 NULL 规则。同步[统计公式](contracts/baseline-statistics.md#已确认的主要统计公式)、Agent 入口和开发说明并清理公式待定表述；不改变样本资格、既有门槛或异常判断规则，阶段／调用具体门槛继续待确认，统计功能尚未实施 |
| 2026-09-24 | 用户确认 Parse、Bind、Execute 首次及续取分别沿用可配置的试行门槛：基础 30 个有效调用样本且 7 个活跃日、P95 为 200 个且 7 日、P99 为 1,000 个且 7 日。按五项分组的整体与各跨天小时范围独立检查，以本次调用的北京时间推算开始日期计活跃日，不跨类别或小时借样本，逐天不套七日门槛；不足仍保留统计及提示，达标只表示满足数量条件，不直接用于整条 SQL 的 activity 异常判断。同步[阶段与调用门槛](contracts/baseline-statistics.md#已确认的阶段与调用样本门槛)、Agent 入口及开发说明；其他质量条件、阶段异常规则和版本衔接仍待确认，未实施统计功能 |
| 2026-09-25 | 用户确认同一次构建的五类计时共用训练窗口和基线版本并统一生效：至少一类有有效样本时可在批次完整、构建及发布检查通过后发布，全部分组不足仍保留统计与不足标记；仅有 Parse／Bind／Execute 调用样本时也可发布，但明确缺少请求整体基线，不直接据此判断整条 SQL 的 activity 异常。五类在整窗均无有效样本时保留原版本；新版本缺失类别不自动用旧统计补齐，旧结果通过历史版本查看。同步[五类统一版本](features/baseline-versions.md#已确认的五类计时统一版本与发布边界)、Agent 入口及开发说明；跨集群构建与生效范围、质量检查细则等继续待确认，未实施发布功能 |
| 2026-09-25 | 用户确认按计算集群独立管理基线：各有当前及历史版本，按自身批次、训练窗口和检查结果分别导入、构建、发布；同一集群内数据库、用户、SQL 分组与五类计时统一发布，统计仍分别计算。119 导入失败不阻止 120 在自身条件满足后更新，整窗五类零有效样本也按集群独立判断；查询显示所选集群及版本的窗口与该集群更新时间。同步[按集群管理版本](features/baseline-versions.md#已确认的按集群独立管理基线版本)、Agent 入口与开发说明；集群／来源标识和具体发布实现仍待落实，未实施功能 |
| 2026-09-25 | 用户确认人工维护固定集群标识和日志来源到集群的映射，导入时显式选择已登记来源；集群用于基线分组及版本，来源用于追溯、同源去重和日志关联，不新增分组维度。文件／目录改名沿用标识，Master 来源变化时人工登记或确认归属，确认为同一逻辑集群则沿用集群标识；来源未登记或映射不明确的文件先补充信息再导入。同步[标识与来源映射](features/log-ingestion.md#已确认的集群标识与日志来源映射)、Agent 入口和开发说明；示例名称未创建为配置，文件身份、变化／重叠文件及实现细节仍待落实 |
| 2026-09-25 | 用户确认指纹失败不自动降级为原文哈希或简单文本指纹：SQL 缺失、不完整或解析器暂不支持语法，最终无法可靠生成结构指纹时暂不训练，解析不支持与实际 SQL 执行错误分别记录；多语句批次部分不可解析时整批暂不训练，不摘取子语句使用整批耗时。能可靠界定影响范围时记录诊断原因、数量与来源位置并继续其他记录，CSV 记录边界损坏仍按文件失败处理。同步[指纹失败处理](features/log-ingestion.md#已确认的指纹生成失败处理)、Agent 入口和开发说明；实际指纹覆盖率及解析器实现未验证，原文与诊断数据存储留存仍待定 |
| 2026-09-25 | 用户确认 SQL 原文在 PostgreSQL 中按完整内容精确去重保存，执行／调用明细引用原文；真实执行和调用各自保留时间、耗时、状态等明细，同一结构指纹下不同参数的原文分别保存，日志绑定参数占位符原样保留。存储去重不替代结构指纹、不合并执行次数或跨分组统计，基线版本不重复复制原文。同步[原文存储方式](contracts/sql-storage.md#已确认的-sql-原文存储方式)、Agent 入口及开发说明，并更新存储方式待定描述；适用于需要保留的明细和可靠原文，留存期限、清理、压缩与表结构仍待定，未实施入库或清理 |
| 2026-09-25 | 用户确认首期暂缓自动清理：程序暂不自动删除执行／调用明细、去重 SQL 原文和历史基线版本，原始 CSV 由人工管理；默认 30 天仅作训练窗口，不作为自动删除界限，已保留的窗口外数据仍可历史回看。首轮真实入库后，根据实际占用、增长和回看需求分别确定留存期限及清理方案。同步[首期留存边界](contracts/sql-storage.md#已确认的首期留存与自动清理边界)、Agent 入口及开发说明；尚未实施数据库容量测量或清理，不构成永久留存承诺 |
| 2026-09-25 | 用户确认首期不自动合并追加／修改文件或跨文件部分重叠内容：可靠确认已导入源文件变更或部分同源日志重复时，记录原因、来源和依据，暂停受影响文件并阻止该集群本批次发布，待人工核对；不覆盖旧数据或提取增量，保留当前基线，其他集群独立处理。同源相同内容且此前成功导入仍跳过，未变化原文件的失败重试仍安全支持；同名、时间相交或 SQL 相同不单独构成判重证据。同步[变更及重叠文件规则](features/log-ingestion.md#已确认的变更及部分重叠文件处理)、Agent 入口及开发说明；具体文件身份、识别方法、覆盖范围和人工恢复流程仍待落实，未实施检测或导入功能 |
| 2026-09-25 | 用户确认指纹算法及函数归一化字典记录规则版本，每次基线构建固定一套规则；更新后，下次构建将选定训练窗口内已保留且可可靠处理的 SQL 原文统一按新规则生成指纹和归组，按既有资格及统计规则生成新基线，通过原有检查后生效。同一版本不混用新旧规则，历史基线保留当时分组、结果和规则依据，原始执行／调用记录及 SQL 原文保持不变，不自动改写全部历史窗口。同步[规则版本与更新处理](features/baseline-versions.md#已确认的归一化规则版本与更新处理)、Agent 入口和开发说明；具体版本标识、关联实现与性能未验证，已将规则版本衔接原则补入 [Issue #1](https://github.com/shenxg13/sql-apm/issues/1) 正文背景，保留其原交付范围及完整契约待确认状态；函数字典具体条目继续在该 Issue 细化，未启动产品实施 |
| 2026-09-25 | 用户确认首期发布采用完整性与计算一致性检查：批次未完成、规则混用、必要结果缺失／保存失败、同类样本计数或统计值一致性检查失败时阻止该集群生效，保留原版本。样本不足及已可靠隔离的问题记录保留原因和提示，不单独拦截；整窗五类零有效样本仍不替换。暂不增加覆盖率、有效样本比例或相对旧版波动的发布阈值。同步[首期发布检查](features/baseline-versions.md#已确认的首期基线发布检查)、Agent 入口及开发说明；具体核对方式、数值容差、报告与检查功能待实现验证，不将发布通过等同于统计稳定或自动异常判定已就绪 |
| 2026-09-25 | 用户确认故障／维护排除时段含开始、不含结束；正耗时样本仅在推算区间有正长度重叠时排除，恰在排除开始结束或排除结束开始不命中。可靠 0 ms 样本按时间点是否落在该时段内判断，开始点命中、结束点不命中，未知耗时不补零。同步[人工排除规则](contracts/training-eligibility.md#故障与维护时段的人工排除)、Agent 入口及开发说明，清理端点待定表述；保持推算性质、其他样本资格及事后重算暂缓边界，排除功能尚未实现 |
| 2026-09-25 | 用户确认每个集群构建开始时固定并记录已完成导入批次及数据范围、训练窗口、黑名单、排除时段、归一化规则与样本门槛等配置，五类计时和三层统计使用相同输入；构建期间新数据及配置变化留待下一次构建，普通变化本身不改写或自动中止本次计算。后续纳入及发布仍遵守原有资格、完整性与空结果规则，明细和原文复用，历史结果保持不变。同步[构建输入与配置固定](features/baseline-versions.md#已确认的构建输入与配置固定)、Agent 入口与开发说明；具体保存、隔离、并发及调度未实现，未新增自动重算流程 |
| 2026-09-25 | 用户确认首期同一集群的导入、构建和发布任务串行，已有任务时新启动提示占用并退出，人工稍后重试，暂不自动排队；忙时拒绝不影响当前任务，不同集群独立，查询继续使用当前生效版本。同集群新到达日志留待后续任务导入，固定输入与既有导入／发布规则继续适用。同步[任务串行与忙时处理](features/baseline-versions.md#已确认的同集群任务串行与忙时处理)、Agent 入口及开发说明，并调整先前构建中新增数据示例以符合串行边界；互斥、占用恢复及资源控制尚未实现或验证 |
| 2026-09-25 | 用户确认日志完整成功导入后，仅基线计算失败或中断时复用导入结果，人工重新发起构建，对选定训练窗口从头计算，首期不实现统计计算断点续算；每次固定并记录实际输入配置，保留失败记录，沿用串行、样本资格、发布检查和零样本规则。同步[基线计算重试范围](features/baseline-versions.md#已确认的基线计算失败重试范围)、Agent 入口及开发说明；导入安全重试要求不变，具体重试入口与中间结果管理未实现，未开展性能测量 |
| 2026-09-25 | 用户说明生产环境至少可提供 8c16g 虚拟机与 500 GB 磁盘，并明确不是硬门禁；记录为可调整的[生产资源参考](decisions/runtime-and-components.md#已知的生产资源参考)，同步 Agent 入口和开发说明，不视为固定资源上限或性能验收阈值。服务部署位置与资源分配尚未落实，每日导入和 30 天窗口重建的目标时长未指定，实际容量、耗时和峰值内存尚未测量 |
| 2026-09-25 | 用户确认首期通过命令行进行日志导入、基线构建、任务状态与诊断结果查询，来源映射、批次文件清单、黑名单和排除时段由本地配置文件维护，具体格式实施时确定；Grafana 继续承担已确认的 SQL 检索、基线与执行历史展示。同步[首期操作入口](features/operator-cli.md#已确认的首期操作入口)、Agent 入口和开发说明，更新操作方式待定表述；命令及配置解析尚未实现，完整流程与分步命令的组织方式、参数与输出继续落实，未增加网页管理或任务管理 API 的首期要求 |
| 2026-09-25 | 用户确认首期提供完整流程命令，在确认批次文件齐全后依次导入、构建、检查并于满足既有发布条件时自动生效；同时保留只导入及复用完整成功导入数据重新构建两种用法。阶段失败停止后续步骤并保留当前基线，继续遵守同集群串行、固定输入、安全重试及整窗五类零样本规则。同步[完整流程与分步使用](features/operator-cli.md#已确认的完整流程与分步使用)、Agent 入口和开发说明；自动生效仅限显式调用流程，未增加调度或自动重试，具体命令及编排尚未实现 |
| 2026-09-25 | 用户确认[首期交付顺序](decisions/project-scope.md#已确认的首期交付顺序)：先完成离线日志导入、指纹、五类计时与三层统计、数据库存储、版本管理及命令行诊断，用现有 119／120 日志核对结果并测量耗时、峰值内存和数据库增长，再接入 SQL 检索和 Grafana 基线／历史展示；展示仍属首期，activity 与定时 SCP 保持后续安排。同步 Agent 入口、README 和开发说明；未开展产品实施，具体任务及验收用例继续在对应 Issue 落实 |
| 2026-09-25 | 用户说明生产为 Kylin V10 SP2、完全离线，明确当前先以可联网 Alma 环境开发，后续通过专门 Issue 实施 Kylin 离线安装方案，不作为当前工作前置条件。只读核对本机为 AlmaLinux 9.8、x86_64，生产架构未提供，未进行下载、安装或兼容验收。同步[开发环境与生产部署安排](decisions/runtime-and-components.md#已确认的当前开发环境与生产部署安排)、Agent 入口、README 和开发说明；Python 3.9.5 与 PostgreSQL 17 要求不变，本次未创建 Kylin 专项 Issue |
| 2026-09-25 | 用户明确目前不需要确认实施细节；当前沟通继续集中业务需求及范围，任务拆分、具体技术方案和实现级验收用例留待对应 Issue。停止推进此前尚未确认的五部分实施任务划分，不将其提升为项目决定；既有业务规则、技术栈及交付顺序保持有效。同步[资料状态与使用约定](decisions/project-scope.md#资料状态与使用约定)和 Agent 入口，未开展产品实施或创建新 Issue |
| 2026-09-25 | 用户确认[其余非函数参数常量保留规则](contracts/sql-fingerprints.md#已确认的其余非函数参数常量保留规则)：已明确归一化范围之外的非函数参数常量保留具体值，值不同则指纹不同，例如 SELECT 计算系数和此前未约定的 CASE 结果值；既有业务值归一化、特殊值保留及原生参数边界继续适用。函数参数的逐项、匹配及默认规则仍由既有字典 Issue 确定。同步 Agent 入口与开发说明，清理相关待定表述；原文不变，入库与检索复用同一规则，未开展实现或扩大 Issue 范围 |
| 2026-09-25 | 按用户明确要求创建并回读 [Issue #2：梳理首版 SQL 黑名单及适用范围](https://github.com/shenxg13/sql-apm/issues/2)，承接已确认名单、候选项与理由、类别变体、是否支持模板排除及批次正反例。初始标签为 status:triage，完整契约确认项未勾选；新增排除项及按 SQL 分组排除五类计时的方案继续待确认。同步[后续梳理入口](contracts/training-eligibility.md#首版黑名单规则的后续梳理)、Agent 入口和开发说明；仅建立需求入口，未开展实现或改动既有业务规则 |
| 2026-09-25 | 用户确认统一黑名单支持语句类别与具体 SQL 模板两类规则，并将 [Issue #2](https://github.com/shenxg13/sql-apm/issues/2) 收窄为只梳理语句类别清单及适用范围；模板条目由用户后续在生产运行中调整，不由该 Issue 预设，也不作为其验收或前置条件。已记录范围变更评论、更新标题与正文并回读核对，保持需求梳理状态及完整契约未确认。同步[能力与维护分工](contracts/training-eligibility.md#已确认的黑名单能力与维护分工)、Agent 入口和开发说明；未新增具体排除条目或实施功能 |
| 2026-09-25 | 按用户授权将详细需求完整迁入主题页，精简 Agent 入口和索引，并改为按任务阅读与单处维护；保留确认来源和未决边界。见[维护方法](methods/knowledge-maintenance.md)及[迁移核对](../docs/reports/knowledge-reorganization-2026-09-25.md)，未改变产品需求或实施 Issue 状态 |
| 2026-09-25 | 用户要求首期面向 HashData，同时在设计中保留其他数据库与跑批系统等非数据库来源的基线接入空间。记录[多类型系统接入扩展约束](decisions/project-scope.md#已确认的多类型系统接入扩展约束)，接入、计时与指纹主题仅增加引用；首期业务规则和交付范围保持原约定，具体扩展机制及未来适配对象仍待落实 |
| 2026-09-25 | 用户将首期时间统计明确为[整体、逐天、逐周、跨周星期、跨天小时五层](contracts/baseline-statistics.md#已确认的时间统计粒度)，沿用各层全部主要指标及北京时间推算开始时间归属；自然周为周一 00:00 至下周一 00:00，窗口首尾不足整周仍保留窗口内样本、标注实际覆盖，不补取窗口外数据。同步分组引用、构建版本及计数核对范围，默认 30 天窗口不变；新增两层样本门槛及异常应用仍待确认，尚未实现 |
| 2026-09-25 | 用户要求逐周与跨周星期也暂定试行门槛，按该要求拟定[首版参数](contracts/baseline-statistics.md#暂定的逐周与跨周星期样本门槛)：基础／P95／P99 沿用 30／200／1,000 个有效样本，逐周各要求该周内至少 3 个活跃日，跨周星期各要求至少 4 个不同自然周出现该星期几的有效样本。各分组、计时类别和分桶独立检查；不足保留统计，边缘周继续标明不足整周。参数可调整、未经生产校准，不改变发布规则或提前确定 activity 参照及异常公式，尚未实现 |
| 2026-09-25 | 用户确认采用[逐周与跨周星期试行门槛](contracts/baseline-statistics.md#暂定的逐周与跨周星期样本门槛)：基础／P95／P99 分别为 30／200／1,000 个有效样本，逐周另需至少 3 个活跃日，跨周星期另需该星期几在至少 4 个不同自然周有有效样本。保留独立检查、不足仍展示及边缘周标注规则；参数可调整、尚未经生产校准，确认不等于功能已经实现 |
| 2026-09-25 | 用户接受[首期保持五个时间统计层次](contracts/baseline-statistics.md#已确认的时间统计粒度)，暂不增加星期 × 小时、业务日历画像或逐月统计；这些仅为后续候选，结合实际数据和业务需要另行评估与确认。既有五层指标、时间归属及试行门槛保持有效 |
| 2026-09-25 | 用户引用逐天门槛建议并回复“你建议的门槛”，按该选择记录[逐天试行参数](contracts/baseline-statistics.md#暂定的逐天样本门槛)：每个分组及计时类别的当天有效样本，基础／P95／P99 分别至少 30／200／1,000 个，不加跨天覆盖要求。各日独立检查、不足仍保留可计算统计；参数可调整，达标仅表示数量条件满足，不自动确定异常应用或额外质量标签，尚未实现 |
| 2026-09-25 | 用户选择[首期先展示指标、自动质量标签后定](contracts/training-eligibility.md#成功长耗时执行的处理)：保存展示 CV、MAD、IQR 等既有统计值和样本不足信息，“高波动／疑似离群”自动标签及阈值留待实际数据评估后另行确定。成功长耗时样本保留规则、样本门槛标记及已知排除原因继续适用；功能尚未实现 |
| 2026-09-25 | 用户确认先完成[数据契约设计](decisions/project-scope.md#已确认的数据契约设计范围)，已创建 [Issue #3](https://github.com/shenxg13/sql-apm/issues/3)：定义逻辑对象、字段约束、来源映射、版本兼容及样例；数据库物理表结构和建表脚本留给后续 Issue。保留 HashData 首期语义与多类型接入边界，详细验收草案在 Issue 中收敛，尚未启动设计交付或产品实现 |
