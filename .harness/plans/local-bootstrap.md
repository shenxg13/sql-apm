# SQL APM 本地仓库初始化计划

## 已授权范围

- 用户于 2026-09-22 明确要求以 agent-harness 为基础创建当前本地仓库。
- 本地目录为 /home/sfmon/code/sql-apm，采用独立 Git 历史和 main 分支。
- 模板固定为 shenxg13/agent-harness 的提交
  `859c39d1697241579b9bde443125477f88b1827c`。
- 保留已保存的 SQL Baseline 原文及其来源信息，将其作为待讨论的项目知识。
- 导入通用流程、知识模板、质量检查、Issue/PR 模板及质量 CI 配置。
- 完成本地质量验证、首次提交和独立本地克隆验证。

用户已明确暂不固定业务架构、技术栈、实现范围、阈值和验收标准；
此前建议不构成已接受的业务方案。项目运行模型继续留待沟通确认。
最初授权范围限本地初始化；后续首次发布授权记录在本计划末尾。

## 初始化交付检查

以下是仓库初始化的技术检查，不定义 SQL APM 产品验收标准。

1. 通用模板文件完整，入口和知识引用有效。
2. 原始文档 Git blob SHA 保持为
   `d5b8c6e4ef8d317110aec7737d49088d4a9308e4`。
3. README、知识索引和任务路由明确当前处于需求沟通阶段。
4. 上游历史计划与报告明确标为上游证据，不混为本项目验证或授权。
5. 使用模板声明的工具版本，通过离线质量检查和已有流程回归。
6. 本地初始化完成时，独立 main 历史没有 GitHub remote；首次提交可在干净本地克隆中验证。

## 实施顺序

1. 读取固定来源，记录文件清单、内容摘要和可执行权限。
2. 导入模板，保留项目已有原始知识，完成必要的项目文档适配。
3. 复用或安装声明的质量工具，保留工具版本合同。
4. 初始化 Git、暂存交付文件，运行本地质量入口并记录结果。
5. 形成首次提交，创建干净本地克隆，核对原文、历史和检查结果。
6. 汇总本地验证证据、已知限制和后续需求讨论入口。

## 验证用例

- 使用 `scripts/quality/check.sh --check-tools-only` 验证工具合同。
- 使用 `scripts/quality/check.sh` 验证 Shell、Markdown、模板和离线流程。
- 使用 `git diff --cached --check` 以及提交后的
  `git show --format= --check HEAD` 检查交付内容。
- 使用 Git blob 摘要核对原文内容，在本地克隆中重复核对。
- 核对根提交、main 分支、remote 列表、提交后工作区和文件权限。
- 验证继承的模拟 GitHub 用例，不调用真实 Issue/PR 写入。

## 成本与证据边界

此任务仅处理小规模模板和文档，性能与恢复判断为 qualitative。
工具下载仅用于仓库检查；业务吞吐、现网采集、SQL 算法和远程 CI 均未验证。
本地自检不等同于独立评审；如进行独立本地评审，另行记录确切提交和结果。

## 首次远程同步扩展（2026-09-22）

用户在完成本地初始化后要求创建 GitHub 仓库并进行首次同步，随后明确选择 Public。

- 目标：shenxg13/sql-apm，公开仓库，默认分支 main。
- 范围：当前已提交的完整本地历史和文件，包括原始 SQL Baseline 文档，
  以及本次必要的协作阶段说明与发布验证记录。
- 本次沿用现有文件集，不新增 LICENSE；许可证选择继续待讨论。
- 按原有开发流程配置六个 status 标签，回读名称与颜色。
- 推送后核对仓库身份、公开属性、默认分支、提交、知识原文及首次质量 CI。
- 初始化发布继续使用 bootstrap 交接，不补造历史 Issue/PR；新需求随后进入 GitHub 工作流。

验证方案：发布前检查文档、模板和离线回归；发布后使用 GitHub CLI 与连接器
分别读取目标，确认本地 main 与远程 main 一致，原文摘要保持不变，CI 成功。
结果记录在[首次发布记录](../../docs/reports/initial-publication.md)。
