---
id: contract.training-eligibility
type: contract
status: active
owners:
  - .project-wiki/contracts/training-eligibility.md
updated: 2026-09-26
sources:
  - path: .project-wiki/log.md
    status: historical
  - path: docs/reports/knowledge-reorganization-2026-09-25.md
    status: current
  - path: docs/reports/statement-category-census-2026-09-26.md
    status: current
related:
  - contract.timing-and-grouping
  - contract.sql-fingerprints
  - contract.baseline-statistics
  - contract.sql-storage
  - feature.sql-search-and-views
confidence: high
---

# 训练资格、黑名单与排除时段

## Summary

保存已确认的纳入规则及排除边界，未确认的新增黑名单条目仍由对应工作处理。
调整训练筛选、单条／整批单位、黑名单或排除时段时阅读。

## Source Of Truth

以下保留原知识索引各节的用户确认、日期、来源状态、证据限制和待定事项。
这些条款定义目标或事实，不表示相关产品功能已经实现；较早条款中的待定表述
应结合明确链接的后续确认阅读，不能覆盖后续已确认规则。
[知识变更记录](../log.md)用于追溯；涉及 Issue 时以其在线正文和评论核对任务契约。

## Contracts

### 已确认的 SQL 纳入与排除策略

- 确认日期：2026-09-22。
- 来源：用户提出采用黑名单策略，除明确排除的 SQL 外，其余都建立基线；随后
  引用控制语句、维护语句及具体 DDL 的讨论，确认“这些都可以列到黑名单里”。
- 来源状态：current；已确认 SQL 纳入原则和下列初始排除项，尚未实现过滤逻辑。
- 采用“默认纳入，明确排除”的原则：未命中黑名单的 SQL 进入基线计算范围。

| 类别 | 当前明确列入黑名单的语句 |
| --- | --- |
| 控制语句 | `SET`、`BEGIN`、`COMMIT` |
| 维护语句 | `VACUUM`、`ANALYZE` |
| 指定 DDL | `CREATE INDEX`（建索引）、`ALTER TABLE`（修改表结构） |

上述名单逐项列举；其他未明确排除的语句按默认纳入原则处理，包括此前讨论过的
`CREATE TABLE AS SELECT`。本次确认不扩展为排除全部 DDL。

黑名单控制是否参与基线计算。被排除语句仍可按执行关联需要用于上下文解析，
混合批次按下述已确认规则判定；日志及执行明细的存储与留存策略继续待定。
进入计算范围不等于已经具备可用基线；基本训练资格及暂定样本门槛见下述规则，
其余异常筛选策略另行确认。

具体配置方式与识别实现留待实施。首版语句类别名单的补充、别名／变体的
业务覆盖由下述 Issue #2 承接；具体 SQL 模板条目按已确认维护分工由用户
在后续生产运行中调整。

关联条款：[已确认的首期留存与自动清理边界](sql-storage.md#已确认的首期留存与自动清理边界)；[已确认的阶段与调用样本门槛](baseline-statistics.md#已确认的阶段与调用样本门槛)。

### 已确认的黑名单能力与维护分工

- 确认日期：2026-09-25。
- 来源：用户明确黑名单可以实现“按语句类别排除”和“按具体 SQL 模板排除”
  两类功能，但 Issue #2 只梳理前者，后者的具体语句由用户后续在生产运行中调整。
- 来源状态：current；已确认两类能力及维护职责，功能尚未实现。
- 首期在同一套黑名单中支持语句类别规则和具体 SQL 模板规则，不另设一套
  独立排除名单。类别规则使用上述已确认名单并由 Issue #2 继续梳理；模板
  规则用于对指定 SQL／完整批次模板的分组排除训练。
- 具体需要加入模板规则的 SQL 由用户结合后续生产运行情况维护，Issue #2
  不收集或预设该名单，也不将它作为类别规则梳理的前置条件或验收内容。
  运行期维护的是模板条目，不表示将模板排除能力移出首期范围。
- 已确认的默认纳入、类别规则下的纯黑名单／混合批次处理、明细保留及历史
  基线不改写等规则继续适用；本次未新增任何语句类别或具体模板条目。
- 进一步的模板匹配、计时适用细则及具体配置实现留待对应功能落实，不因本次
  维护分工确认而认定此前所有细节建议已获逐项确认，也不扩展 Issue #2 范围。

关联条款：[已确认的执行历史记录范围](../features/sql-search-and-views.md#已确认的执行历史记录范围)；[已确认的基线更新与历史版本保留](../features/baseline-versions.md#已确认的基线更新与历史版本保留)。

### 首版黑名单规则的维护入口

- 确认日期：2026-09-25。
- 来源：用户要求创建 Issue，并随后明确只梳理语句类别黑名单。
- 来源状态：current；已更新并回读核对
  [Issue #2：梳理首版 SQL 语句类别黑名单及适用范围](https://github.com/shenxg13/sql-apm/issues/2)。
- 该 Issue 仅承接类别清单、类别候选及排除理由、别名／变体覆盖，以及单条／
  纯黑名单批次／混合批次的类别规则正反例。2026-09-26 用户已确认交付契约并
  授权实施；保守名单与判定边界见下节，后续扩展仍在在线契约中确认。
- 按具体 SQL 模板排除的能力已确认，但其条目梳理和匹配功能设计／实现均不由
  Issue #2 交付。用户运行中维护条目的安排，不改变既有语句类别名单。
- 这里只记录已确认分工与任务入口。实时正文、评论及交付状态以 GitHub 为准，
  后续 Issue 工作前回读，不在本地维护其正文或状态镜像。本次未实现黑名单
  过滤器、配置解析或基线构建功能。

### 首版类别边界与保守维护规则

- 确认日期：2026-09-26。
- 来源：用户确认 Issue #2 实施计划并要求“日志核查尽量全面，名单要稍微保守一些，
  不确定的不要放入黑名单”。本节据此细化已有七项的边界，未新增排除类别。
- 来源状态：current；交付业务规则和诊断证据，产品过滤器尚未实现。
- [全量核查报告](../../docs/reports/statement-category-census-2026-09-26.md)记录
  119／120 全部 46 个文件的观察、候选、来源与限制；观察频率不是排除依据。
- 以下规则适用于能可靠识别的**顶层完整语句**。大小写、普通空白和注释不改变
  类别；字符串、引号标识符、美元引用体内的关键字不形成额外命中。
  词法前缀只是调查线索，不能代替语法和执行资格判定。

| 已确认类别 | 业务解释与排除理由 | 同一语句的覆盖边界 | 不据此扩大排除 |
| --- | --- | --- | --- |
| `SET` | 调整运行参数，作为会话／事务执行上下文，不单独训练其参数设置耗时 | 普通运行参数的 `SET`、`SET SESSION`、`SET LOCAL`，以及运行参数写法 `SET TIME ZONE`、`SET NAMES`、`SET SCHEMA`、`SET SEED` | `RESET`、`SELECT set_config(...)`、`UPDATE pg_settings`；身份、约束及事务特征设置见下述暂缓边界 |
| `BEGIN` | 开始事务，单独作为控制步骤排除 | `BEGIN`、`BEGIN WORK`、`BEGIN TRANSACTION` 及该命令的事务模式选项 | `START TRANSACTION` 尚不自动折叠；过程体中的 `BEGIN` 不作为独立控制语句 |
| `COMMIT` | 提交事务，单独作为控制步骤排除，不以耗时短为前提 | `COMMIT`、`COMMIT WORK`、`COMMIT TRANSACTION` | `END`、`COMMIT PREPARED` 另列候选；不按 `COMMIT` 前缀覆盖所有事务命令 |
| `VACUUM` | 空间回收／维护，其单条维护耗时按已确认决定排除 | 确认属于 `VACUUM` 的 `FULL`、`FREEZE`、`VERBOSE`、`ANALYZE` 选项和合法目标范围 | `REINDEX`、`CLUSTER`、`CHECKPOINT`；不把所有维护任务或维护时段内的 SQL 当成此类别 |
| `ANALYZE` | 收集优化器统计信息，其单条维护耗时排除 | `ANALYZE`、`ANALYZE VERBOSE`，合法表／列目标；方言专有选项须能可靠确认仍属该命令 | 英式拼写 `ANALYSE` 暂缓自动折叠；`EXPLAIN ANALYZE` 属于 `EXPLAIN` |
| `CREATE INDEX` | 创建索引，为指定的结构维护类别 | `CREATE INDEX` 与 `CREATE UNIQUE INDEX`；其他选项以目标方言有效且确属建索引为前提 | `ALTER INDEX`、`REINDEX`、`CREATE TABLE` 及 `CREATE TABLE AS SELECT` |
| `ALTER TABLE` | 修改表定义，为已确认的指定 DDL 类别；不按具体子动作耗时决定排除 | 明确属于 `ALTER TABLE` 的加／删列、改名、设置及分区等合法子动作，含 `IF EXISTS`、`ONLY` 等修饰 | `ALTER INDEX`、`ALTER VIEW`、`ALTER DATABASE`；不能扩大为全部 `ALTER` 或全部 DDL |

七项排除意图来自此前用户确认；表内 SQL 语义依据见报告的固定上游源码和官方文档。
业务理由是对已确认控制／维护分工的解释，不是从频率或耗时推断其没有业务价值。
方言语法被上游接受，不等于已验证目标 HashData 构建可成功执行该语法。

#### 不确定类别与别名的处理

首版保持七项。对未逐项确认的类别或会扩大排除范围的别名，不因语义近似或
在日志里出现就启用。下表的“暂缓”由用户本次统一的保守原则授权，**不表示
用户逐项确认了这些候选的业务价值或永久拒绝**。它们不因本类别规则排除，
但仍须满足其他训练资格；后续新增须有业务依据、正反例并逐项确认。

| 候选／边界 | 调查结论与当前决定 | 重新评估条件 |
| --- | --- | --- |
| `END`、`START TRANSACTION`、`ANALYSE` | 上游语法证实与已有命令存在别名／等效关系；业务覆盖尚未逐项确认，暂缓自动折叠 | 用户确认相应别名覆盖及目标方言识别范围 |
| `SET ROLE`、`SET SESSION AUTHORIZATION`、`SET TRANSACTION`、`SET SESSION CHARACTERISTICS AS TRANSACTION`、`SET CONSTRAINTS` | 具有身份、事务或约束语义，不能仅靠首词 `SET` 合并为普通运行参数设置；保守暂缓 | 明确哪些设置作为独立训练对象排除，以及混合批次预期 |
| `ROLLBACK`／`ABORT`、保存点命令、两阶段事务命令 | 控制用途是候选理由，但单条控制耗时及回滚归属可能有分析价值；暂缓 | 用户确认控制类别的完整范围；与执行失败资格分别解释 |
| `RESET`、`DISCARD`、`SHOW`、预备语句／游标管理命令 | 可能属于连接初始化、清理或客户端调用；暂缓，不统一排除 | 区分管理命令与实际执行／取数，不能丢掉 `EXECUTE`／`FETCH` 业务耗时 |
| `LOCK`、`EXPLAIN`、`REINDEX`、`CLUSTER`、`CHECKPOINT` | 有控制、诊断或维护用途，但耗时可能正是关注对象；暂缓 | 逐类给出业务理由，尤其区分 `EXPLAIN` 与实际执行的 `EXPLAIN ANALYZE` |
| `CREATE/DROP TABLE`、外部表、视图、序列、权限及注释命令 | 可以是正常业务批次的一部分，未采纳“一并排除 DDL／管理语句”的扩展 | 如有新增排除需求，逐项确认，不依名称扩大 |
| `SELECT`、`WITH`、DML、`COPY`、过程或函数调用 | 未采纳按整个类别排除；监控查询、特定函数或短／长耗时不改变此结论 | 特定业务 SQL 的排除由用户后续维护模板条目 |
| 未识别、截断、编码异常或分句不可靠文本 | 不推断为黑名单；也不把“未命中”误当成可训练 | 按既有训练资格处理可靠性，明确原因，无法归属时留在批次层面 |

#### 类别判定验收样例

下列 SQL 均为人工合成，仅走查规则，不执行数据库操作。“不因类别排除”不
保证成功执行、计时完整、关联可靠或满足故障时段及其他训练资格。

| 编号 | 合成输入 | 类别规则预期与边界 |
| --- | --- | --- |
| C01 | `SET LOCAL work_mem = '16MB';` | 排除单条运行参数设置 |
| C02 | `BEGIN WORK;` | 排除单条事务开始 |
| C03 | `COMMIT TRANSACTION;` | 排除单条普通事务提交 |
| C04 | `VACUUM ANALYZE demo;` | 排除一个维护命令，不算两个子语句 |
| C05 | `ANALYZE VERBOSE demo;` | 排除统计收集命令 |
| C06 | `CREATE UNIQUE INDEX demo_i ON demo (id);` | 排除建索引类别 |
| C07 | `ALTER TABLE demo ADD COLUMN flag integer;` | 排除改表类别 |
| C08 | `SET work_mem = '16MB'; BEGIN; COMMIT;` | 纯黑名单批次，整批排除 |
| C09 | `SET work_mem = '16MB'; BEGIN; INSERT INTO demo VALUES (1); COMMIT;` | 不因类别排除；按整批计时，包含控制语句贡献 |
| C10 | `ALTER TABLE demo ADD COLUMN flag integer; ANALYZE demo; INSERT INTO demo VALUES (1, 2);` | 不因类别排除；维护与业务混合批次，不拆分时长 |
| C11 | `CREATE TABLE demo_copy AS SELECT 1 AS id;` | 不因类别排除；不扩展到全部 DDL |
| C12 | `EXPLAIN ANALYZE SELECT 1;` | 不因类别排除；不能因内含 `ANALYZE` 命中维护类别 |
| C13 | `SELECT 'SET; COMMIT', $$ALTER TABLE demo$$;` | 不因类别排除；字符串内关键字与分号不是子语句 |
| C14 | `/* SELECT */ sEt work_mem = '16MB'; -- INSERT` | 排除；普通注释不改变实际 `SET` 类别 |
| C15 | `END;` / `START TRANSACTION;` / `ANALYSE demo;` | 三个独立别名案例均不因当前类别规则排除，覆盖暂缓 |
| C16 | `COMMIT PREPARED 'demo_tx';` | 不因类别排除；与普通 `COMMIT` 分开 |
| C17 | `RESET ALL;` / `SET ROLE NONE;` / `SET TRANSACTION READ ONLY;` | 三个独立案例均不因当前类别规则排除，扩展暂缓 |
| C18 | `SELECT set_config('work_mem', '16MB', false);` | 不因类别排除；函数效果不把 `SELECT` 变为 `SET` |
| C19 | `ALTER INDEX demo_i RENAME TO demo_j;` | 不因类别排除；不是 `ALTER TABLE` |
| C20 | `SET work_mem = '16MB'; SELECT 'cut` | 文本不完整，不声称纯黑名单；按可靠性资格暂不训练 |
| C21 | `; /* comment */ ;` | 没有可识别执行语句，不虚构样本，也不声称黑名单命中 |
| C22 | `DO $$BEGIN PERFORM 1; END$$;` | 不因类别排除；过程体不是独立 `BEGIN`／`END` 批次 |
| C23 | `ROLLBACK;` / `SHOW work_mem;` / `DEALLOCATE ALL;` / `LOCK TABLE demo IN ACCESS SHARE MODE;` | 四个独立新增候选案例均暂缓，不因类别排除 |
| C24 | `SET work_mem = '16MB'; SELECT 1;` | 不因首条 `SET` 排除整批 |
| C25 | `SET work_mem = '16MB'; END;` | `END` 暂缓，不能认定为纯黑名单批次；其他资格仍独立检查 |

类别判断与其他规则的关系继续遵守本页单条／批次和基本资格条款：

- 一次整批执行仍是一个候选样本，不将控制或维护子句的耗时分摊、扣除或补造。
- 类别规则只影响训练资格，不删除源日志、原文、已保留明细或历史基线；可靠归属
  的排除原因继续保留，无法可靠归属的问题留在批次层面，见[SQL 留存](sql-storage.md)。
- 请求整体、Execute 首次、Execute 续取、Parse、Bind 独立统计，见[计时分类](timing-and-grouping.md)。
  本次日志类别计数包括阶段、内部与重复出现文本，不能相加为完整执行次数。
- 构建使用固定输入和规则配置，普通更新不改写运行中的构建或已保存历史，见
  [构建与版本](../features/baseline-versions.md)。

### 已确认的单条语句与多语句批次

- 确认日期：2026-09-23。
- 来源：用户对单条／批次分别建立基线及混合批次黑名单的完整提议，明确选择
  “采用上述规则，混合批次可建整体基线”。
- 来源状态：current；已确认训练单位、指纹粒度及黑名单应用原则，尚未实现。
- 单条 SQL 按单条生成结构指纹并建立基线。对于能可靠识别完整成功执行及整批
  耗时的多语句请求，保留语句顺序，对整个批次生成结构指纹，与单条 SQL 分开
  统计；不将整批耗时分配给各个子语句，也不借此生成子语句的独立耗时样本。
- 一次符合训练资格的整批执行计为一个有效执行样本，不按其中语句条数增加
  样本数；真实重复执行的同一批次仍逐次计数。基线继续按计算集群、数据库、
  执行用户及相应结构指纹分组。
- 纯黑名单语句组成的批次排除；包含未被黑名单排除的业务语句的混合批次，
  允许按整批进入候选集，其耗时包含控制和维护语句的贡献，不因首条为 SET
  就排除整批，也不从总时长中推测扣除黑名单语句耗时。
- 例如 `SET → BEGIN → INSERT → COMMIT` 或 `ALTER TABLE → ANALYZE → INSERT`
  在满足整批训练资格时可以纳入；只有 `SET → BEGIN → COMMIT` 的批次排除。
  原黑名单逐项定义保持不变，不扩展为排除全部 DDL。
- 状态不明、计时归属不清或执行关联不可靠的记录暂不训练。按整批纳入仍须
  应用既有的故障／维护时段排除及其他训练资格；混合批次许可不绕过这些规则。
- 对批次以选定完成记录的结束时间减整批 duration 推算批次开始时间，沿用
  北京时间的开始日期／小时归属和完整区间重叠排除，不声称恢复子语句起止时间。
- 完成记录选择、单条／整批计时映射及具体指纹规范化算法仍待落实。WHERE 条件及
  INSERT／UPDATE 业务值的处理，以及 SET 配置值和 LIMIT／OFFSET 数量的保留，见下述已确认
  规则；普通格式及注释差异的原则也已在下文确认，具体语法识别仍待实现。

关联条款：[已确认的基线分组维度](timing-and-grouping.md#已确认的基线分组维度)；[已确认的业务常量归一化](sql-fingerprints.md#已确认的业务常量归一化)；[已确认的执行配置与取数限制](sql-fingerprints.md#已确认的执行配置与取数限制)；[已确认的普通格式与注释处理](sql-fingerprints.md#已确认的普通格式与注释处理)；[已确认的时间口径](timing-and-grouping.md#已确认的时间口径)。

### 已确认的训练样本基本资格

- 确认日期：2026-09-22。
- 来源：用户对成功执行进入训练候选集、失败或不可靠执行不参与正常耗时训练的
  三类基本筛选原则回复“确认”。
- 来源状态：current；已确认训练资格原则，尚未实现状态判定、事件关联或样本筛选。
- 下列规则应用于未被 SQL 黑名单排除的执行。

| 执行情况 | 处理原则 |
| --- | --- |
| 成功完成，耗时完整，能可靠识别为一次执行 | 进入训练候选集 |
| 明确失败、取消或超时 | 不参与正常耗时基线训练 |
| 状态不明、耗时缺失或执行关联不可靠 | 暂不参与训练 |

进入候选集不等于最终用于训练。成功执行不能仅根据耗时长短直接认定为正常或异常。
故障与维护时段的人工排除、成功长耗时执行的处理见下述已确认规则；疑似离群及
高波动的自动标签已明确留待实际数据评估后确定，首期展示既有统计指标和样本
不足信息。具体异常判断方法另行确定，统计公式采用统计契约中的已确认定义。
训练窗口及暂定样本门槛见下述规则。
能可靠识别并归属到 SQL 分组的被排除执行，按下述已确认的执行历史范围
保留和展示；具体存储方式及保留期限继续另行讨论。

首期执行耗时及开始时间采用下述已确认的时间估算简化假设。完成记录的识别、
执行状态判定及其他必要的日志关联，留待结合日志格式与样本落实。

关联条款：[已确认的训练窗口](baseline-statistics.md#已确认的训练窗口)；[暂定的样本门槛与不足处理](baseline-statistics.md#暂定的样本门槛与不足处理)；[已确认的阶段与调用样本门槛](baseline-statistics.md#已确认的阶段与调用样本门槛)；[已确认的执行历史记录范围](../features/sql-search-and-views.md#已确认的执行历史记录范围)；[已确认的 SQL 原文存储方式](sql-storage.md#已确认的-sql-原文存储方式)；[已确认的首期留存与自动清理边界](sql-storage.md#已确认的首期留存与自动清理边界)；[已确认的首期执行时间估算假设](timing-and-grouping.md#已确认的首期执行时间估算假设)。

### 故障与维护时段的人工排除

- 确认日期：2026-09-22；2026-09-25 补充端点与零耗时样本的匹配规则。
- 来源：在询问首期是否需要手动指定“计算集群＋故障或维护时间段”，将受影响的
  执行样本排除出训练时，用户回答“需要”；随后对“SQL 执行区间与排除时段有
  重叠，就不参与训练”的匹配规则回复“确认”。
  对已生成基线因补录排除时段而重算的问题，用户明确表示“这个暂时不需要，
  因为sql日志是后录入的，不是实时采集”。2026-09-25，用户对排除时段采用
  “含开始、不含结束”、正耗时样本仅端点相接不排除、可靠零耗时按时间点
  判断的建议回复“确认”。
- 来源状态：current；这是已确认的首期功能范围，尚未实现操作入口或排除逻辑。
- 首期支持手动指定计算集群及故障或维护时间段，将该范围内受影响的执行样本
  排除出基线训练，避免已知故障或维护影响正常耗时基线。
- 这项排除也适用于已成功完成、原本符合基本训练资格的执行。
- 对指定计算集群，SQL 执行区间与排除时段有重叠即排除出训练；不要求 SQL 的
  开始和结束时间都落在排除时段内。
- 首期以完成记录的结束时间及下述方法推算的开始时间形成执行区间，保留其
  估算性质；不为该区间额外要求精确匹配开始日志。
- 因此，排除时段开始前已启动且期间仍在运行的 SQL、期间启动而结束于其后的
  SQL，以及完整跨越该时段的 SQL，均适用此规则。
- 例如排除时段为 10:00～10:30，执行区间为 09:50～10:10 的 SQL 也排除出训练。
- 首期按日志事后录入、离线构建基线的流程推进；在本轮构建前准备好日志及已知
  故障或维护排除时段，并在训练样本筛选时应用排除规则。
- 已生成基线因事后补录排除时段而触发的手动或自动重算流程暂缓，不列为首期
  必需功能。日常新增日志后的基线更新见下述已确认的更新与历史版本保留规则。
- 故障或维护时段按下述已确认的北京时间口径填写，采用“含开始、不含结束”
  的区间 `[排除开始, 排除结束)`。正耗时样本只有与该时段有正长度的时间重叠
  才因该时段排除：`样本开始 < 排除结束` 且 `样本结束 > 排除开始`。
- 恰好在排除开始时结束，或恰好在排除结束时开始的正耗时样本，不因该时段
  排除。例如排除时段为 10:00～10:30，09:50～10:00 和 10:30～10:40 不命中；
  09:50～10:01、10:20～10:40 命中。是否最终可训练仍按其他既定资格判断。
- 日志可靠记录为 0 ms 的样本按其时间点 `t` 判断：`排除开始 <= t < 排除结束`
  时排除；上述例子中 10:00 的零耗时样本命中，10:30 的不命中。不对零耗时
  样本套用正长度重叠条件而漏掉时段内的点样本，也不将未知耗时补为零。
- 边界判断沿用已确认的推算开始和日志结束时间，不把它们提升为精确实测开始
  或 activity 时间；各计时类别的样本单位及其他训练资格继续适用。排除时段
  已确认通过本地配置文件维护，具体格式、时间表示及匹配实现仍待落实，
  不因本条启动事后补录时段的追溯重算。

关联条款：[已确认的时间口径](timing-and-grouping.md#已确认的时间口径)；[已确认的首期执行时间估算假设](timing-and-grouping.md#已确认的首期执行时间估算假设)；[已确认的同集群任务串行与忙时处理](../features/baseline-versions.md#已确认的同集群任务串行与忙时处理)。

### 成功长耗时执行的处理

- 确认日期：2026-09-22（保留成功长耗时样本）；2026-09-25（自动质量标签后定）。
- 来源：针对通过基本筛选、未命中已确认排除规则的成功长耗时执行先保留参与
  统计，不仅因耗时长就自动剔除，疑似离群或高波动标记规则另行细化的建议，
  用户回复“确认”。2026-09-25，在询问首期是否先展示 CV、MAD、IQR 等统计值
  及样本不足信息、自动“高波动／疑似离群”标签和阈值待实际数据评估后确定时，
  用户选择“先展示指标，自动标签后定”。
- 来源状态：current；已确认首期样本处理及质量展示边界，筛选与展示尚未实现；
  自动质量标签留待后续工作。
- 成功完成、耗时完整且执行关联可靠的记录，在未命中 SQL 黑名单及故障或维护
  时段等已确认排除规则时，保留参与基线统计，即使其耗时明显长于同组其他执行。
- 首期不单凭耗时长或处于分布最慢端就自动剔除该执行。自动离群剔除算法留待
  后续结合实际数据分析确定，不将原始资料中的相关建议直接作为首期过滤规则。
- 首期保留并展示已确认的统计指标（包括 CV、MAD、IQR 等）及适用的样本不足
  信息，暂不自动生成“高波动／疑似离群”判断性标签。实际数据评估后，再确认
  是否引入自动标签及其具体方法和阈值；这不是首期实施的前置确认项。
- 既有样本数量与覆盖条件的达标／不足标记继续执行，已知训练排除原因继续
  保留。自动质量标签的延期不取消这些信息，也不把未产生标签解释为已经判定
  正常或稳定。标记本身不自动排除样本，成功长耗时执行沿用上述保留规则。
- 保留参与统计不等于已认定该次执行正常，也不代表其所在基线已具备异常判断
  的可用性。慢执行可能影响尾部统计，波动较大的分组仍需单独评估可用性，
  具体质量规则继续另行确认。

关联条款：[暂定的样本门槛与不足处理](baseline-statistics.md#暂定的样本门槛与不足处理)；[已确认的基线更新与历史版本保留](../features/baseline-versions.md#已确认的基线更新与历史版本保留)。

## Workflows

按任务涉及的边界补读：

- [计时分类、分组与时间归属](timing-and-grouping.md)。
- [SQL 结构指纹与归一化](sql-fingerprints.md)。
- [统计指标、训练窗口与样本门槛](baseline-statistics.md)。
- [SQL 原文、明细与留存](sql-storage.md)。
- [SQL 检索、Grafana 与历史展示](../features/sql-search-and-views.md)。

## Failure Modes

不能把候选建议、历史观察或待验证实现当成已确认且已交付的行为；
本页各节保留的限制及关联条款共同约束相应任务。

## Update Rules

本主题的确认内容只在本页维护；其他入口保留链接。跨主题变更同步实际受影响的
条款，按[知识维护方法](../methods/knowledge-maintenance.md)记录来源与变更。

## Open Questions

类别新增、别名及特殊 SET 覆盖按上述保守边界暂缓；模板细则及具体实现仍按各条款分别落实。自动“高波动／
疑似离群”标签及其阈值已明确后续结合实际数据另行确定。各节已有待定说明继续有效。
